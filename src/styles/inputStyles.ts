import { StyleSheet } from "react-native";

export const inputStyles = StyleSheet.create({
  inputPrimaryStyle: {
    width: "100%",
    backgroundColor: "#f5f5f5",
    padding: 10,
  },
  outlinePrimayStyle: {
    borderWidth: 0,
    borderRadius: 15,
  },
});
